# Timestamp Stock Converter

## How to use it.
  - Clone the repo
    ```bash
    $ git clone git@github.com:masmarkus/Converter.git
    $ cd Converter
    ```
  - Open your terminal and run :
    ```bash
    $ python conv.py "<inputFilename>"
    ```
    
